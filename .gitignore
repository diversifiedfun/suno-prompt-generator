node_modules/
.DS_Store
*.log
dist/

# Secrets — never commit
.env
.env.*

# Store build artifact (regenerate via store/package.sh)
store/*.zip

# Local submission prep — keep private, not for the public repo
store/listing.md
store/submission-checklist.md
store/permissions-justifications.md

# Internal — never commit to the PUBLIC repo
HANDOFF.md
.claude/
.ship/

# Nova Reign — unreleased artist content (lyrics/masters), keep out of the public repo
nova-reign/
